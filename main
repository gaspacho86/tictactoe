    public static void main(String[] args) {
        createBattlefield();
        printBattlefield();
        while (!gameOver) {
            gamePlay();
            checkWinnerStatus(battlefield);
        }
    }
